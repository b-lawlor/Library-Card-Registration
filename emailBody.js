const getPlainBody = (name, library) => {
  var body = "plain text email body";

  return body;
}

const getHtmlBody = (name, library) => {
  var body = "<h1>HTML Formated Email Body</h1>";

  return body
}